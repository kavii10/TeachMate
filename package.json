{
  "name": "teachmate-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently --names api,web --prefix-colors blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "npm --prefix backend start",
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "check": "npm --prefix backend run check && npm --prefix frontend run check",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
